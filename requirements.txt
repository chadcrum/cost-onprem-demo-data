# Core dependencies (required for all scripts)
psycopg2-binary>=2.9
redis>=5.0
requests>=2.28

# NISE-based generators (only needed for scripts that use the NISE pipeline)
koku-nise>=4.0

# Implicit stdlib: base64, json, os, sys, time, uuid, subprocess, tempfile, tarfile, pathlib
